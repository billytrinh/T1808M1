#include <stdio.h>
int main(){
	int n;
	printf("nhap so can kiem tra n=\n");
	scanf("%d",&n);
		
	for ( int i  = 2 < (n-1) ; i==i+1 )
		{
		}if ( n%i == 0 ) 
		{ printf ("khong la so nguyen to\n",n);
		break;
		}
		else {
		Printf("la so nguyen to");}
}
