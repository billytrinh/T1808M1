#include<stdio.h>

int main(){
	int n;
	printf ("\nnhap so tu nhien n:");
	scanf ("%d", &n);
	printf ("cac so tu nhien nho hon %d la :",n);
	for (int i=0;i<n;i++)
	  if (i<n)
	     printf ("%d\t", i);
	     
		 
return 0;
	
}
